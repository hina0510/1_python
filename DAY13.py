# <DAY13>
# set {} 순서없고 중복없는 자료형! 집합연산 시 용이!
# .add(x) 세트에 x 추가
# .remove(x) 세트에 x 제거
# 교집합 &, 합집합 |, 차집합 -

# 딕셔너리 자료끼리 관계를 맺어줄 때 사용
# 단어(key), 뜻(value)
# d = {1:"one"} # 단어(key) : 뜻(value)

# key indexing key를 통해 value 에 접근

# d = {1:"one", 2:"two", 3:"three"}

# 기존에 존재하는 key 값에 value 매칭 : 갱신
# d[1]= 1
# 기존에 존재하지 않는 key 값에 value 매칭 : 추가
# d[4] = 4
# 딕셔너리 자료 삭제
# del d[1]

# for i in d :
#     print(i, d[i]) # key값에 접근, key값과 value값

# dictionary 자료형 사용 시 key, value 뭐 들어있는지 주석처리!!


























































































